server:
  port: ${PORT:5000}

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 200000

ribbon:
  ReadTimeout: 100000
  ConnectTimeout: 30000

zuul:
  ignoredServices: '*'
  routes:
    registration-service:
      serviceId: registration-service
      path: /registration/**

eureka:
  instance:
    hostname: auth-icu.herokuapp.com

secret: jazaradebuggers

# Enable multipart uploads
spring:
  servlet:
    multipart:
      enabled: true
    # Threshold after which files are written to disk.
    file-size-threshold: 2KB
    # Max file size.
    max-file-size: 10MB
    # Max Request Size
    max-request-size: 20MB
    ## File Storage Properties
    # All files uploaded through the REST API will be stored in this directory
    file.upload-dir: /app

  datasource:
    url: jdbc:postgresql://ec2-44-206-214-233.compute-1.amazonaws.com:5432/d4m6dlvhs1172j?createDatabaseIfNotExist=true
    username: kwjckzrpftvxzb
    password: ENC(zYWvfmhbRyLlOwfBjVE+rSia2pkWcnQD7p6W8vwJI89LJSvg/oYis2xKUgwzA97w90exGNJD2+71+LFAfhsCU4PAXNcvtPewKTtU750fK5U=)
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update